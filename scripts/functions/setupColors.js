"use_strict";

let red;
let green;
let blue;

//maby add let colors = []; in setup.js and push all colors to that array.
const setupColors = () => {
  red = color(255, 0, 0);
  green = color(0, 255, 0);
  blue = color(0, 0, 255);
};
